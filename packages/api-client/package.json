{
  "name": "@product-management/api-client",
  "version": "1.0.0",
  "description": "Shared API client for internal projects",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "barrelsby --directory src/endpoints --delete && tsc --build",
    "clean": "rm -rf dist",
    "barrel": "barrelsby --directory src/endpoints --delete"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@faker-js/faker": "10.0.0",
    "barrelsby": "^2.8.1"
  }
}